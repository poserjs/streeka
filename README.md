# Streeka

This repository contains a starter Next.js (TypeScript) application using the `app/` directory.

## Getting Started

```bash
npm install
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000) in your browser.
